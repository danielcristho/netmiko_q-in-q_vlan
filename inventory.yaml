Q-IN-Q:
  - host: 172.16.1.1
    int_conf: 
      - interface: ether2
        ip_address: 10.0.1.1/30
    
    vlan_conf:
      - interface: vlan 10

      - interface: vlan 123
        ip_address: 11.0.1.1/30

  - host: 172.16.1.2
    int_conf:
      - interface: ether1
        ip_address: 10.0.1.2/30
    
    vlan_conf:
      - interface: vlan 10

      - interface: vlan 123
        ip_address: 11.0.1.2/30